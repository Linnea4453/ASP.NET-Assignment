@model IdentityRole

@foreach (var user in ViewBag.Users)
{
    <div>@user.DisplayName, @user.Role</div>
}


@Html.DropDownListFor(model => model.Id, new SelectList(ViewBag.Roles, "Id", "Name"), "Välj roll")

